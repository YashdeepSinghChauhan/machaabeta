#posts.transitions-enabled
	- @posts.each do |post|
		.box.panel.panel-default
			= link_to (image_tag post.image.url), post
			%h2= link_to post.title, post
			%p.user
			Submitted by
			.round-image-50-search
				= image_tag (post.user.avatar.url(:thumb))